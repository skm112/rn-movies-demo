import { StyleSheet } from 'react-native'

export const MoviesStyles = (colors) => StyleSheet.create({
    container: {
        flexGrow: 1,
        justifyContent: 'flex-start',
        backgroundColor: colors.bg
    }
})
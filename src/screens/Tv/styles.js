import { StyleSheet } from 'react-native'

export const TvStyles = (colors) => StyleSheet.create({
    container: {
        flexGrow: 1,
        justifyContent: 'flex-start',
        backgroundColor: colors.bg
    }
})